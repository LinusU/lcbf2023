interface Theme {
  primary: string;
  onPrimary: string;
  background: string;
  onBackground: string;
}

export const theme: Theme = {
  primary: 'orange',
  onPrimary: 'white',
  background: 'white',
  onBackground: 'black',
}
